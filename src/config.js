// Preview mode: when true, the site is locked on the homepage.
// Navigation links, footer links, and CTA buttons that would lead
// the visitor to other pages are hidden or disabled.
//
// Set to false to re-enable the full multi-page site.
export const PREVIEW_MODE = true;
